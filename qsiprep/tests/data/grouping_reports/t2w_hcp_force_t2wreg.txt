DWI grouping for sub-01
=======================

Output "sub-01" (MultipartID auto+concat+0 [inferred]): 2 series
  Distortion group sub-01_dir-AP (PE j-, TRT 0.05s):
    - sub-01_dir-AP_dwi.nii.gz
    corrected by: auto+t2wreg [cli-override]
    (also eligible: auto+pepolar+j)
  Distortion group sub-01_dir-PA (PE j, TRT 0.05s):
    - sub-01_dir-PA_dwi.nii.gz
    corrected by: auto+t2wreg [cli-override]
    (also eligible: auto+pepolar+j)

Fieldmap estimations:
  auto+pepolar+j [inferred]: PEPOLAR (reverse phase-encoding) (not used)
    - sub-01_dir-AP_dwi.nii.gz
    - sub-01_dir-PA_dwi.nii.gz
    phase encoding axes: j (bidirectional)
  auto+t2wreg [cli-override]: T2w registration (T2Wreg)
    - sub-01_T2w.nii.gz

Processing preview: FSL path (TOPUP fieldmap estimation + eddy HMC/SDC)
-----------------------------------------------------------------------

Output "sub-01" (2 series):
  1. Each series is denoised on its own, then all 2 series are concatenated. (--denoise-after-combining reverses this order.)
  2. eddy corrects head motion and eddy currents. The selected correction is a T2w registration (T2Wreg), which only the TORTOISE path implements: distortion is NOT corrected on this path.
  3. The corrected series is written as one output file.
  !! ERROR [anat-sdc-unsupported]: Estimation 'auto+t2wreg' is a T2w registration (T2Wreg), which only the TORTOISE path implements. Use --hmc-model tortoise to run it.

Processing preview: TORTOISE path (DIFFPREP HMC + DRBUDDI SDC)
--------------------------------------------------------------

Output "sub-01" (2 series):
  1. Each series is denoised on its own, then all 2 series are concatenated. (--denoise-after-combining reverses this order.)
  2. DIFFPREP corrects head motion and eddy currents separately for each distortion group (sub-01_dir-AP, sub-01_dir-PA).
  3. DIFFPREP registers the b=0 image to the T2w (sub-01_T2w.nii.gz) to estimate and correct distortion (T2Wreg).
  4. The corrected series is written as one output file.

Processing preview: Two-stage path (TOPUP + eddy, then DRBUDDI or T2Wreg refinement)
------------------------------------------------------------------------------------

Output "sub-01" (2 series):
  1. Each series is denoised on its own, then all 2 series are concatenated. (--denoise-after-combining reverses this order.)
  2. The selected correction is a T2w registration (T2Wreg), which only the TORTOISE path implements: processing follows the FSL path with NO distortion correction.
  3. The corrected series is written as one output file.
  !! ERROR [anat-sdc-unsupported]: Estimation 'auto+t2wreg' is a T2w registration (T2Wreg), which only the TORTOISE path implements. Use --hmc-model tortoise to run it.
