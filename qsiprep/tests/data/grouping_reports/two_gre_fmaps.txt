DWI grouping for sub-01
=======================

Output "sub-01_dir-AP" (MultipartID auto+concat+0 [inferred]): 2 series
  Distortion group sub-01_dir-AP_run-1 (PE j-, TRT 0.05s):
    - sub-01_dir-AP_run-1_dwi.nii.gz
    corrected by: auto+fmap+sub-01_acq-run1 [intendedfor]
  Distortion group sub-01_dir-AP_run-2 (PE j-, TRT 0.08s):
    - sub-01_dir-AP_run-2_dwi.nii.gz
    corrected by: auto+fmap+sub-01_acq-run2 [intendedfor]

Fieldmap estimations:
  auto+fmap+sub-01_acq-run1 [intendedfor]: GRE phase difference
    - sub-01_acq-run1_magnitude1.nii.gz
    - sub-01_acq-run1_phasediff.nii.gz
  auto+fmap+sub-01_acq-run2 [intendedfor]: GRE phase difference
    - sub-01_acq-run2_magnitude1.nii.gz
    - sub-01_acq-run2_phasediff.nii.gz

Notes:
  WARNING [inferred-concat-merge]: Distortion groups 'sub-01_dir-AP_run-1' and 'sub-01_dir-AP_run-2' are corrected by different fieldmaps but share a session and shim settings, so their outputs will be concatenated. Use MultipartID (or separate_all_dwis) to keep them separate.

Processing preview: FSL path (TOPUP fieldmap estimation + eddy HMC/SDC)
-----------------------------------------------------------------------

Output "sub-01_dir-AP" (2 series):
  1. Each series is denoised on its own, then all 2 series are concatenated. (--denoise-after-combining reverses this order.)
  2. A fieldmap is computed from the GRE phase difference acquisition (sub-01_acq-run1_magnitude1.nii.gz, sub-01_acq-run1_phasediff.nii.gz).
  3. A fieldmap is computed from the GRE phase difference acquisition (sub-01_acq-run2_magnitude1.nii.gz, sub-01_acq-run2_phasediff.nii.gz).
  4. eddy corrects head motion and eddy currents; the fieldmap is applied to unwarp the results.
  5. The corrected series is written as one output file.

Processing preview: TORTOISE path (DIFFPREP HMC + DRBUDDI SDC)
--------------------------------------------------------------

Output "sub-01_dir-AP" (2 series):
  1. Each series is denoised on its own, then all 2 series are concatenated. (--denoise-after-combining reverses this order.)
  2. DIFFPREP corrects head motion and eddy currents separately for each distortion group (sub-01_dir-AP_run-1, sub-01_dir-AP_run-2).
  3. A fieldmap is computed from the GRE phase difference acquisition and applied to unwarp the DIFFPREP output.
  4. A fieldmap is computed from the GRE phase difference acquisition and applied to unwarp the DIFFPREP output.
  5. The corrected series is written as one output file.

Processing preview: Two-stage path (TOPUP + eddy, then DRBUDDI or T2Wreg refinement)
------------------------------------------------------------------------------------

Output "sub-01_dir-AP" (2 series):
  1. Each series is denoised on its own, then all 2 series are concatenated. (--denoise-after-combining reverses this order.)
  2. Without a PEPOLAR fieldmap the DRBUDDI second stage adds nothing: processing follows the FSL path (fieldmap-based correction).
  3. The corrected series is written as one output file.
  !! WARNING [mixed-non-pepolar]: Estimation 'auto+fmap+sub-01_acq-run1' is not PEPOLAR; the DRBUDDI second stage only refines PEPOLAR corrections, so 'sub-01_dir-AP' will get single-stage correction.
  !! WARNING [mixed-non-pepolar]: Estimation 'auto+fmap+sub-01_acq-run2' is not PEPOLAR; the DRBUDDI second stage only refines PEPOLAR corrections, so 'sub-01_dir-AP' will get single-stage correction.
