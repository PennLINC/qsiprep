DWI grouping for sub-01
=======================

Output "sub-01_dir-AP" (MultipartID auto+concat+0 [inferred]): 2 series
  Distortion group sub-01_dir-AP_run-1 (PE j-, TRT 0.05s):
    - sub-01_dir-AP_run-1_dwi.nii.gz
    corrected by: auto+fmap+sub-01_acq-run1 [intendedfor]
  Distortion group sub-01_dir-AP_run-2 (PE j-, TRT 0.08s):
    - sub-01_dir-AP_run-2_dwi.nii.gz
    corrected by: auto+fmap+sub-01_acq-run2 [intendedfor]

Fieldmap estimations:
  auto+fmap+sub-01_acq-run1 [intendedfor]: GRE phase difference
    - sub-01_acq-run1_magnitude1.nii.gz
    - sub-01_acq-run1_phasediff.nii.gz
  auto+fmap+sub-01_acq-run2 [intendedfor]: GRE phase difference
    - sub-01_acq-run2_magnitude1.nii.gz
    - sub-01_acq-run2_phasediff.nii.gz

Processing preview: eddy + TOPUP (--hmc-method eddy --sdc-method topup)
-----------------------------------------------------------------------

Output "sub-01_dir-AP" (2 series):
  1. Each series is denoised on its own. The 2 series span 2 correction units; each unit is concatenated and corrected in its own pipeline.
  2. Correction unit 'sub-01_dir-AP_run-1' (sub-01_dir-AP_run-1_dwi.nii.gz):
  3. A fieldmap is computed from the GRE phase difference acquisition (sub-01_acq-run1_magnitude1.nii.gz, sub-01_acq-run1_phasediff.nii.gz).
  4. eddy corrects head motion and eddy currents; the fieldmap is applied to unwarp the results.
  5. Correction unit 'sub-01_dir-AP_run-2' (sub-01_dir-AP_run-2_dwi.nii.gz):
  6. A fieldmap is computed from the GRE phase difference acquisition (sub-01_acq-run2_magnitude1.nii.gz, sub-01_acq-run2_phasediff.nii.gz).
  7. eddy corrects head motion and eddy currents; the fieldmap is applied to unwarp the results.
  8. The corrected results of the 2 units are resampled onto one grid and concatenated into one output file.

Processing preview: TORTOISE + DRBUDDI (--hmc-method tortoise --sdc-method drbuddi)
-----------------------------------------------------------------------------------

Output "sub-01_dir-AP" (2 series):
  1. Each series is denoised on its own. The 2 series span 2 correction units; each unit is concatenated and corrected in its own pipeline.
  2. Correction unit 'sub-01_dir-AP_run-1' (sub-01_dir-AP_run-1_dwi.nii.gz):
  3. TORTOISE DIFFPREP corrects head motion and eddy currents.
  4. A fieldmap is computed from the GRE phase difference acquisition and applied to unwarp the DIFFPREP output.
  5. Correction unit 'sub-01_dir-AP_run-2' (sub-01_dir-AP_run-2_dwi.nii.gz):
  6. TORTOISE DIFFPREP corrects head motion and eddy currents.
  7. A fieldmap is computed from the GRE phase difference acquisition and applied to unwarp the DIFFPREP output.
  8. The corrected results of the 2 units are resampled onto one grid and concatenated into one output file.

Processing preview: eddy + TOPUP→DRBUDDI (--hmc-method eddy --sdc-method topup+drbuddi)
---------------------------------------------------------------------------------------

Output "sub-01_dir-AP" (2 series):
  1. Each series is denoised on its own. The 2 series span 2 correction units; each unit is concatenated and corrected in its own pipeline.
  2. Correction unit 'sub-01_dir-AP_run-1' (sub-01_dir-AP_run-1_dwi.nii.gz):
  3. Without a PEPOLAR fieldmap the DRBUDDI second stage adds nothing: processing follows the FSL path (fieldmap-based correction).
  4. Correction unit 'sub-01_dir-AP_run-2' (sub-01_dir-AP_run-2_dwi.nii.gz):
  5. Without a PEPOLAR fieldmap the DRBUDDI second stage adds nothing: processing follows the FSL path (fieldmap-based correction).
  6. The corrected results of the 2 units are resampled onto one grid and concatenated into one output file.
  !! WARNING [mixed-non-pepolar]: Estimation 'auto+fmap+sub-01_acq-run1' is not PEPOLAR; the DRBUDDI second stage only refines PEPOLAR corrections, so 'sub-01_dir-AP' will get single-stage correction.
  !! WARNING [mixed-non-pepolar]: Estimation 'auto+fmap+sub-01_acq-run2' is not PEPOLAR; the DRBUDDI second stage only refines PEPOLAR corrections, so 'sub-01_dir-AP' will get single-stage correction.

Processing preview: SHORELine + DRBUDDI (--hmc-method shoreline --sdc-method drbuddi)
-------------------------------------------------------------------------------------

Output "sub-01_dir-AP" (2 series):
  1. Each series is denoised on its own. The 2 series span 2 correction units; each unit is concatenated and corrected in its own pipeline.
  2. Correction unit 'sub-01_dir-AP_run-1' (sub-01_dir-AP_run-1_dwi.nii.gz):
  3. SHORELine iteratively corrects head motion by registering each volume to a 3dSHORE signal-model prediction (eddy currents are not corrected).
  4. A fieldmap is computed from the GRE phase difference acquisition and applied to unwarp the corrected series.
  5. Correction unit 'sub-01_dir-AP_run-2' (sub-01_dir-AP_run-2_dwi.nii.gz):
  6. SHORELine iteratively corrects head motion by registering each volume to a 3dSHORE signal-model prediction (eddy currents are not corrected).
  7. A fieldmap is computed from the GRE phase difference acquisition and applied to unwarp the corrected series.
  8. The corrected results of the 2 units are resampled onto one grid and concatenated into one output file.
