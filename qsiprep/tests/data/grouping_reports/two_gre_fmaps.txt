DWI grouping for sub-01
=======================

Output "sub-01_dir-AP_run-1" (MultipartID auto+concat+0 [inferred]): 1 series
  Distortion group sub-01_dir-AP_run-1 (PE j-, TRT 0.05s):
    - sub-01_dir-AP_run-1_dwi.nii.gz
    corrected by: auto+fmap+sub-01_acq-run1 [intendedfor]

Output "sub-01_dir-AP_run-2" (MultipartID auto+concat+1 [inferred]): 1 series
  Distortion group sub-01_dir-AP_run-2 (PE j-, TRT 0.08s):
    - sub-01_dir-AP_run-2_dwi.nii.gz
    corrected by: auto+fmap+sub-01_acq-run2 [intendedfor]

Fieldmap estimations:
  auto+fmap+sub-01_acq-run1 [intendedfor]: GRE phase difference
    - sub-01_acq-run1_magnitude1.nii.gz
    - sub-01_acq-run1_phasediff.nii.gz
  auto+fmap+sub-01_acq-run2 [intendedfor]: GRE phase difference
    - sub-01_acq-run2_magnitude1.nii.gz
    - sub-01_acq-run2_phasediff.nii.gz

Processing preview: FSL path (TOPUP fieldmap estimation + eddy HMC/SDC)
-----------------------------------------------------------------------

Output "sub-01_dir-AP_run-1" (1 single series):
  1. The series is denoised.
  2. A fieldmap is computed from the GRE phase difference acquisition (sub-01_acq-run1_magnitude1.nii.gz, sub-01_acq-run1_phasediff.nii.gz).
  3. eddy corrects head motion and eddy currents; the fieldmap is applied to unwarp the results.
  4. The corrected series is written as one output file.

Output "sub-01_dir-AP_run-2" (1 single series):
  1. The series is denoised.
  2. A fieldmap is computed from the GRE phase difference acquisition (sub-01_acq-run2_magnitude1.nii.gz, sub-01_acq-run2_phasediff.nii.gz).
  3. eddy corrects head motion and eddy currents; the fieldmap is applied to unwarp the results.
  4. The corrected series is written as one output file.

Processing preview: TORTOISE path (DIFFPREP HMC + DRBUDDI SDC)
--------------------------------------------------------------

Output "sub-01_dir-AP_run-1" (1 single series):
  1. The series is denoised.
  2. DIFFPREP corrects head motion and eddy currents.
  3. A fieldmap is computed from the GRE phase difference acquisition and applied to unwarp the DIFFPREP output.
  4. The corrected series is written as one output file.

Output "sub-01_dir-AP_run-2" (1 single series):
  1. The series is denoised.
  2. DIFFPREP corrects head motion and eddy currents.
  3. A fieldmap is computed from the GRE phase difference acquisition and applied to unwarp the DIFFPREP output.
  4. The corrected series is written as one output file.

Processing preview: Two-stage path (TOPUP + eddy, then DRBUDDI or T2Wreg refinement)
------------------------------------------------------------------------------------

Output "sub-01_dir-AP_run-1" (1 single series):
  1. The series is denoised.
  2. Without a PEPOLAR fieldmap the DRBUDDI second stage adds nothing: processing follows the FSL path (fieldmap-based correction).
  3. The corrected series is written as one output file.
  !! WARNING [mixed-non-pepolar]: Estimation 'auto+fmap+sub-01_acq-run1' is not PEPOLAR; the DRBUDDI second stage only refines PEPOLAR corrections, so 'sub-01_dir-AP_run-1' will get single-stage correction.

Output "sub-01_dir-AP_run-2" (1 single series):
  1. The series is denoised.
  2. Without a PEPOLAR fieldmap the DRBUDDI second stage adds nothing: processing follows the FSL path (fieldmap-based correction).
  3. The corrected series is written as one output file.
  !! WARNING [mixed-non-pepolar]: Estimation 'auto+fmap+sub-01_acq-run2' is not PEPOLAR; the DRBUDDI second stage only refines PEPOLAR corrections, so 'sub-01_dir-AP_run-2' will get single-stage correction.
