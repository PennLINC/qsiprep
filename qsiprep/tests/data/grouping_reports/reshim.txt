DWI grouping for sub-01
=======================

Output "sub-01_acq-A" (MultipartID auto+concat+0 [inferred]): 2 series
  Distortion group sub-01_acq-A_dir-AP (PE j-, TRT 0.05s, shimmed):
    - sub-01_acq-A_dir-AP_dwi.nii.gz
    corrected by: auto+pepolar+shim1+j [inferred]
  Distortion group sub-01_acq-A_dir-PA (PE j, TRT 0.05s, shimmed):
    - sub-01_acq-A_dir-PA_dwi.nii.gz
    corrected by: auto+pepolar+shim1+j [inferred]

Output "sub-01_acq-B_dir-AP" (MultipartID auto+concat+1 [inferred]): 1 series
  Distortion group sub-01_acq-B_dir-AP (PE j-, TRT 0.05s, shimmed):
    - sub-01_acq-B_dir-AP_dwi.nii.gz
    corrected by: nothing (no fieldmap found)

Fieldmap estimations:
  auto+pepolar+shim1+j [inferred]: PEPOLAR (reverse phase-encoding)
    - sub-01_acq-A_dir-AP_dwi.nii.gz
    - sub-01_acq-A_dir-PA_dwi.nii.gz
    phase encoding axes: j (bidirectional)

Notes:
  WARNING [session-multiple-shims]: 2 different shim settings found in one session. Series are only combined for fieldmap estimation within a matching shim setting. Use ignore_shims to override.

Processing preview: FSL path (TOPUP fieldmap estimation + eddy HMC/SDC)
-----------------------------------------------------------------------

Output "sub-01_acq-A" (2 series):
  1. Each series is denoised on its own, then all 2 series are concatenated. (--denoise-after-combining reverses this order.)
  2. TOPUP estimates the susceptibility field from b=0 images spanning 2 distortion groups (2 rows in the acquisition-parameters file).
  3. eddy corrects head motion, eddy currents, and susceptibility distortion in one model, using the TOPUP field. Every volume is assigned to its distortion group in the eddy index file.
  4. The corrected series is written as one output file.

Output "sub-01_acq-B_dir-AP" (1 single series):
  1. The series is denoised.
  2. eddy corrects head motion and eddy currents. No fieldmap is available and this subject has no T2w image: susceptibility distortion is NOT corrected.
  3. The corrected series is written as one output file.
  !! WARNING [no-sdc]: Output 'sub-01_acq-B_dir-AP' has no fieldmap and this subject has no T2w image (or the series lacks PhaseEncodingDirection): no susceptibility distortion correction will be performed.

Processing preview: TORTOISE path (DIFFPREP HMC + DRBUDDI SDC)
--------------------------------------------------------------

Output "sub-01_acq-A" (2 series):
  1. Each series is denoised on its own, then all 2 series are concatenated. (--denoise-after-combining reverses this order.)
  2. DIFFPREP corrects head motion and eddy currents separately for each distortion group (sub-01_acq-A_dir-AP, sub-01_acq-A_dir-PA).
  3. DRBUDDI estimates distortion along the j axis from the blip-up (sub-01_acq-A_dir-PA) and blip-down (sub-01_acq-A_dir-AP) data and applies the correction to every volume.
  4. The corrected series is written as one output file.

Output "sub-01_acq-B_dir-AP" (1 single series):
  1. The series is denoised.
  2. DIFFPREP corrects head motion and eddy currents.
  3. No fieldmap is available and this subject has no T2w image: susceptibility distortion is NOT corrected.
  4. The corrected series is written as one output file.
  !! WARNING [no-sdc]: Output 'sub-01_acq-B_dir-AP' has no fieldmap and this subject has no T2w image (or the series lacks PhaseEncodingDirection): no susceptibility distortion correction will be performed.

Processing preview: Two-stage path (TOPUP + eddy, then DRBUDDI or T2Wreg refinement)
------------------------------------------------------------------------------------

Output "sub-01_acq-A" (2 series):
  1. Each series is denoised on its own, then all 2 series are concatenated. (--denoise-after-combining reverses this order.)
  2. TOPUP estimates an initial susceptibility field from the b=0 images (as in the FSL path).
  3. eddy corrects head motion and eddy currents using the TOPUP field.
  4. DRBUDDI re-estimates distortion along the j axis from the eddy-corrected blip-up/blip-down dMRI series, refining the TOPUP correction.
  5. The corrected series is written as one output file.

Output "sub-01_acq-B_dir-AP" (1 single series):
  1. The series is denoised.
  2. No fieldmap is available and this subject has no T2w image: susceptibility distortion is NOT corrected.
  3. The corrected series is written as one output file.
  !! WARNING [no-sdc]: Output 'sub-01_acq-B_dir-AP' has no fieldmap and this subject has no T2w image (or the series lacks PhaseEncodingDirection): no susceptibility distortion correction will be performed.
