DWI grouping for sub-01
=======================

Output "sub-01_dir-AP" (MultipartID auto+concat+0 [inferred]): 1 series
  Distortion group sub-01_dir-AP (PE j-, TRT 0.05s):
    - sub-01_dir-AP_dwi.nii.gz
    corrected by: auto+fmap+sub-01 [intendedfor]

Fieldmap estimations:
  auto+fmap+sub-01 [intendedfor]: GRE phase difference
    - sub-01_magnitude1.nii.gz
    - sub-01_magnitude2.nii.gz
    - sub-01_phasediff.nii.gz

Processing preview: FSL path (TOPUP fieldmap estimation + eddy HMC/SDC)
-----------------------------------------------------------------------

Output "sub-01_dir-AP" (1 single series):
  1. The series is denoised.
  2. A fieldmap is computed from the GRE phase difference acquisition (sub-01_magnitude1.nii.gz, sub-01_magnitude2.nii.gz, sub-01_phasediff.nii.gz).
  3. eddy corrects head motion and eddy currents; the fieldmap is applied to unwarp the results.
  4. The corrected series is written as one output file.

Processing preview: TORTOISE path (DIFFPREP HMC + DRBUDDI SDC)
--------------------------------------------------------------

Output "sub-01_dir-AP" (1 single series):
  1. The series is denoised.
  2. DIFFPREP corrects head motion and eddy currents.
  3. A fieldmap is computed from the GRE phase difference acquisition and applied to unwarp the DIFFPREP output.
  4. The corrected series is written as one output file.

Processing preview: Two-stage path (TOPUP + eddy, then DRBUDDI refinement)
--------------------------------------------------------------------------

Output "sub-01_dir-AP" (1 single series):
  1. The series is denoised.
  2. Without a PEPOLAR fieldmap the DRBUDDI second stage adds nothing: processing follows the FSL path.
  3. The corrected series is written as one output file.
  !! WARNING [mixed-non-pepolar]: Estimation 'auto+fmap+sub-01' is not PEPOLAR; the DRBUDDI second stage only refines PEPOLAR corrections, so 'sub-01_dir-AP' will get single-stage correction.
