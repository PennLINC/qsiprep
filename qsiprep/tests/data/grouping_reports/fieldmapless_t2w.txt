DWI grouping for sub-01
=======================

Output "sub-01_dir-AP" (MultipartID auto+concat+0 [inferred]): 1 series
  Distortion group sub-01_dir-AP (PE j-, TRT 0.05s):
    - sub-01_dir-AP_dwi.nii.gz
    corrected by: auto+t2wreg [inferred]

Fieldmap estimations:
  auto+t2wreg [inferred]: T2w registration (T2Wreg)
    - sub-01_T2w.nii.gz

Processing preview: eddy + TOPUP (--hmc-method eddy --sdc-method topup)
-----------------------------------------------------------------------

Output "sub-01_dir-AP" (1 single series):
  1. The series is denoised.
  2. eddy corrects head motion and eddy currents. The selected correction is a T2w registration (T2Wreg), which only the TORTOISE path implements: distortion is NOT corrected on this path.
  3. The corrected series is written as one output file.
  !! WARNING [anat-sdc-unsupported]: Estimation 'auto+t2wreg' is a T2w registration (T2Wreg), which only DIFFPREP implements. On this path 'sub-01_dir-AP' gets no susceptibility distortion correction.

Processing preview: DIFFPREP + DRBUDDI (--hmc-method diffprep --sdc-method drbuddi)
-----------------------------------------------------------------------------------

Output "sub-01_dir-AP" (1 single series):
  1. The series is denoised.
  2. DIFFPREP corrects head motion and eddy currents.
  3. DIFFPREP registers the b=0 image to the T2w (sub-01_T2w.nii.gz) to estimate and correct distortion (T2Wreg).
  4. The corrected series is written as one output file.

Processing preview: eddy + TOPUP→DRBUDDI (--hmc-method eddy --sdc-method topup+drbuddi)
---------------------------------------------------------------------------------------

Output "sub-01_dir-AP" (1 single series):
  1. The series is denoised.
  2. The selected correction is a T2w registration (T2Wreg), which only the TORTOISE path implements: processing follows the FSL path with NO distortion correction.
  3. The corrected series is written as one output file.
  !! WARNING [anat-sdc-unsupported]: Estimation 'auto+t2wreg' is a T2w registration (T2Wreg), which only DIFFPREP implements. On this path 'sub-01_dir-AP' gets no susceptibility distortion correction.

Processing preview: SHORELine + DRBUDDI (--hmc-method shoreline --sdc-method drbuddi)
-------------------------------------------------------------------------------------

Output "sub-01_dir-AP" (1 single series):
  1. The series is denoised.
  2. SHORELine iteratively corrects head motion by registering each volume to a 3dSHORE signal-model prediction (eddy currents are not corrected).
  3. The selected correction is a T2w registration (T2Wreg) estimation, which the SHORELine path does not implement: distortion is NOT corrected on this path.
  4. The corrected series is written as one output file.
