version: 2
channels:
  - conda-forge
  - defaults
dependencies:
  - python-spams<=2.6.1
  - python=3.7
  - pip>=20.1  # pip is needed as dependency

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - doc
