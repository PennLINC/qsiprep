version: 2

build:
  os: ubuntu-22.04
  apt_packages:
    - blas
    - lapack
  tools:
    python: "3.8"

sphinx:
  configuration: docs/conf.py

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - doc
